This directory contains the encryption keys that we use to test
with Cucumber to an alternative IDP provider.

The keys will match up with the SP metadata XML X.509 certificate that we provide to that IDP.
See also the contents of the "metadata" folder.

