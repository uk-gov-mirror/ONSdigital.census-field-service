This directory contains our SP metadata which has to be provided to
an IDP (such as samltest.id) so that they recognise our application
and can decrypt our messages.

sp-meta-local.xml	- metdata for running locally
sp-meta-dev.xml		- metadata for running in GCP DEV environment
sp-meta-test.xml	- metadata for running in GCP TEST environment

